// define and export a function  for creating a corn seed
// named createCorn. do not use the function keyword use arrow 
// function syntax. the function should return an object with the
// following properties: type, height, output. 
// the createCorn function should return an array with 
// two identical objects in it.
export const createCorn = () => {
   return [ {
        type: "corn",
        height: 180,
        output: 6
    },
    {
        type: "corn",
        height: 180,
        output: 6
},
]
}

